{# src/InstantMovie/BackendBundle/Resources/views/Default/historialUplaod.html.twig #}
{% extends '::frontend.html.twig' %}

{% block title%} Selecionar Modo {% endblock %}
{% block id 'historialUpload' %}

{% block contenido %}

<ul class="nav nav-tabs nav-perfil">
  <li ><a href="{{path('usuario_perfil')}}">Datos del Perfil</a></li>
  <li class="active"><a href="{{path('pagina_historial',{'user_id': app.user.id})}}">Historial</a></li>
</ul>

<div class="row div-historial">
  <div class="col-sm-3 historialNavbar">
    <div class="sidebar-nav">
      <div class="navbar navbar-default" role="navigation">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <span class="visible-xs navbar-brand">Sidebar menu</span>
        </div>
        <div class="navbar-collapse collapse sidebar-navbar-collapse ">
          <ul class="nav navbar-nav">
            <li><a class="btn btn-lg btn " role="button" href="{{path('historial_peliculas',{'user_id': app.user.id})}}">Ver Peliculas Vistas</a></li>
            <li><a class="btn btn-lg btn " role="button" href="{{path('pagina_historial', {'user_id': app.user.id})}}">Ver Comentarios y Puntuaciones</a></li>
            <li><a class="btn btn-lg btn active" role="button" href="{{path('historial_upload', {'user_id': app.user.id})}}">Ver Peliculas Subidas <span class="glyphicon glyphicon-eye-open"</span></a></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
    </div>
  
  <div class="col-sm-9">
    <h1>Gestion de Historial</h1>
    <h3>Bienvenido a la seccion de gestion de tu historial de visualizaciones, aqui podras consultar y gestionarlos contenidos vistos.</h3>

        {% for pelicula in historial %}
            <ul>
                <li>has subido la pelicula {{pelicula.title}} </li>
            </ul>
        {% endfor %}
  </div>
</div> 
    {% endblock %}


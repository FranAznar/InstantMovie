{# src/InstantMovie/BackendBundle/Resources/views/Default/portada.html.twig #}
{% extends '::frontend.html.twig' %}

{% block title%} INSTANT MOVIE {% endblock %}
{% block id 'portada' %}

{% block contenido %}
    <div id= "menuLateral" class="panel">
        <h3>novedades</h3>
        <div id= "Comentarios" class="panel">
           {% for novedades in historial %} <p class="panel comment">{{novedades.User}} <span class ="glyphicon glyphicon-arrow-right"></span>{{novedades.Movie}}: {{novedades.Comment}} <span class="glyphicon glyphicon-comment"</span></p>{% endfor %}
        </div>
        <div id= "Puntuaciones" class="panel">
            {% for upload in uploads %}<p class="panel comment">{{upload.SubidaPor}}<span class ="glyphicon glyphicon-arrow-right"></span> ha subido {{upload.Title}} <span class="glyphicon glyphicon-ok"></span></p>{% endfor %}
           {% for novedades in historial %} <p class="panel comment">{{novedades.User}}<span class ="glyphicon glyphicon-arrow-right"> </span> puntua con un {{novedades.Valoration}}/10 {{novedades.Movie}} <span class="glyphicon glyphicon-ok"</span></p>{% endfor %}
            
        </div>

    </div>
    <div id= "trailer" class="panel">
        <div id= "cajaTrailer" class="panel">
            <iframe src="http://www.youtube.com/embed/M4sagNn4BNU" width="98%" height="98%" frameborder="0" allowfullscreen></iframe>
        </div>
        <div id= "cajaSynapsis" class="panel">
            <h3>Synapsis</h3>
        </div>
    </div>
    <div id= "noticias" class="panel">
        <h3>noticias</h3>
    </div>

{% endblock %}













